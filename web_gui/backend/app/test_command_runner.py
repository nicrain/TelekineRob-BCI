# Tests for command_runner.py — currently empty after legacy TCP arg test removed.
# TCP args (tcp_host / tcp_port) were removed from EegConfig during the gtec-only
# refactor; the test that verified they don't leak into launch commands is
# no longer applicable.
