adapter:
  input: lsl
  lsl_stream_type: EEG
  lsl_timeout: 8.0
  lsl_source_id: ""
policy_cfg:
  # Available: ei (β/(α+θ)), tbr (θ/β), alpha (α)
  policy: tbr
output:
  udp_host: 127.0.0.1
  udp_port: 5005
  hz: 20.0
  verbose: true
