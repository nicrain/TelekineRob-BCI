1. headband 过来的lsl流里, gpype已经对信号做过了预处理, 但是hybrid black的却没有这个预处理(unicornPy没有此功能), 这个怎么办? 
- 核心问题：两条设备路径的预处理不对称

  - Headband:    BCICore8 → [Bandpass 0.5-45Hz] → [Notch 48-52Hz] → LSL → Welch PSD → Policy
  - Hybrid Black: UnicornPy → (原始ADC，无滤波) → LSL → Welch PSD → Policy

- 三个具体影响：

  1. DC 漂移：Hybrid Black 原始数据没有 0.5 Hz 高通，DC 偏移会污染 delta (1-4 Hz) 和 theta (4-8 Hz) 频段 → TBR policy 的 theta_beta 比率会偏高
  2. 50 Hz 工频干扰：Hybrid Black 没有陷波滤波器 → 泄漏到 gamma 频段（但 theta/beta/alpha 不受影响）
  3. 校准参数不通用：TbrPolicy 的 offset=0.207, scale=2.215 是从 headband 数据标定的，直接用于 Hybrid Black 原始数据会导致 speed_intent 分布完全不同